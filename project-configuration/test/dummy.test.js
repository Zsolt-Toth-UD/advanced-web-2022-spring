describe('Dummy Test Suite', () => {
  it('tests nothing', () => {
    // given
    const expected = true;
    // when
    const actual = true;
    // then
    expect(actual).toBe(expected);
  });
});
